import Ember from 'ember';

export default Ember.Component.extend({
  actions: {

    findRentals(rental, city) {
      var rental_list = [];
      forEach()
      if(rental.city.name === this.city.name) {
        rental_list.push(rental)
      }
    }
  }
});
